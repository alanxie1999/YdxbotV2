{
    # 通用配置版本
    "version": "2.5.1",

    # 监听与开奖来源（所有账号默认共用，可在账号配置中覆盖）
    "groups": {
        "zq_group": [],
        "zq_bot": 0
    },

    # 朱雀公共接口（每个账号自己的 cookie/x_csrf 放到账号配置）
    "zhuque": {
        "api_url": "https://zhuque.in/api/user/getInfo?"
    },

    # 押注时序配置
    "betting": {
        "prompt_wait_sec": 1.2,
        "predict_timeout_sec": 8.0,
        "click_interval_sec": 0.45,
        "click_timeout_sec": 6.0
    },

    # 更新配置（私有仓库才需要）
    "update": {
        "github_token": ""
    }
}
